#include <inttypes.h>
#include "pin_names.h"

const uint8_t row_gpios[NUM_ROWS] = {ROW0_GPIO, ROW1_GPIO, ROW2_GPIO, ROW3_GPIO, ROW4_GPIO, ROW5_GPIO};

const uint8_t col_gpios[NUM_COLS] = {COL0_GPIO, COL1_GPIO, COL2_GPIO, COL3_GPIO};